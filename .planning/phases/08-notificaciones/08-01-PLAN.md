---
phase: 08-notificaciones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Layout.jsx
  - src/components/NotificationBell.jsx
  - src/components/NotificationPanel.jsx
autonomous: false
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-04]

must_haves:
  truths:
    - "A bell icon with unread count badge is visible in the header navigation bar for all authenticated users"
    - "Tapping the bell opens a slide-out drawer listing all notifications in reverse chronological order"
    - "Unread notifications have a distinct visual style (dot indicator) versus read notifications"
    - "Tapping a notification marks it as read and removes the dot"
    - "A 'Marcar todo como leido' button marks all notifications as read and clears the badge count"
    - "Empty state shows 'No tienes notificaciones' centered message"
    - "Unread count badge updates on panel open/close and after marking as read"
  artifacts:
    - path: "src/components/NotificationBell.jsx"
      provides: "Bell icon with unread count badge, toggles panel"
      exports: ["default"]
    - path: "src/components/NotificationPanel.jsx"
      provides: "Slide-out drawer with notification list, mark-read, empty state"
      exports: ["default"]
    - path: "src/components/Layout.jsx"
      provides: "Renders NotificationBell in header bar"
      contains: "NotificationBell"
  key_links:
    - from: "src/components/Layout.jsx"
      to: "src/components/NotificationBell.jsx"
      via: "import and render in header div"
      pattern: "import NotificationBell"
    - from: "src/components/NotificationBell.jsx"
      to: "src/components/NotificationPanel.jsx"
      via: "renders NotificationPanel when open"
      pattern: "NotificationPanel"
    - from: "src/components/NotificationBell.jsx"
      to: "src/lib/notificationPersistence.js"
      via: "getUnreadCount for badge"
      pattern: "getUnreadCount"
    - from: "src/components/NotificationPanel.jsx"
      to: "src/lib/notificationPersistence.js"
      via: "getUserNotifications, markNotificationRead, markAllNotificationsRead"
      pattern: "getUserNotifications"
---

<objective>
Add in-app notification bell icon with unread badge to the header bar, and a slide-out notification panel (drawer) where users can view all notifications and mark them as read.

Purpose: Complete the notification flow — Phases 5 and 7 already create notifications on setback/resume; this phase makes them visible to players.
Output: NotificationBell.jsx (bell + badge), NotificationPanel.jsx (drawer), updated Layout.jsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-notificaciones/08-CONTEXT.md

<interfaces>
<!-- Notification persistence API (already implemented, Phase 5) -->

From src/lib/notificationPersistence.js:
```javascript
// All functions take supabaseClient as first arg
export async function getUserNotifications(supabaseClient, userId, { limit = 50 } = {})
// Returns: { success: boolean, data?: object[], error?: string }
// Each notification: { id, tournament_id, user_id, message, type, read, created_at }

export async function getUnreadCount(supabaseClient, userId)
// Returns: { success: boolean, count?: number, error?: string }

export async function markNotificationRead(supabaseClient, notificationId)
// Returns: { success: boolean, error?: string }

export async function markAllNotificationsRead(supabaseClient, userId)
// Returns: { success: boolean, error?: string }
```

From src/context/AuthContext.jsx:
```javascript
// useAuth() provides: { session, profile, isSyncing, ... }
// profile.id = user UUID for notification queries
```

From src/lib/supabaseClient.js:
```javascript
export { supabase } // singleton client
```

Notification types in the system:
- 'setback' — court paused (message includes court name + reason)
- 'schedule_change' — court resumed (message includes next match time)
- 'general' — generic notifications
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotificationBell and NotificationPanel components</name>
  <files>src/components/NotificationBell.jsx, src/components/NotificationPanel.jsx</files>
  <read_first>
    - src/lib/notificationPersistence.js
    - src/context/AuthContext.jsx
    - src/components/Layout.jsx
    - src/components/TournamentActive/SetbackFormModal.jsx (portal pattern reference)
  </read_first>
  <action>
Create two new components:

**NotificationBell.jsx** — Bell icon button rendered in the header:
- Import `useAuth` from `../context/AuthContext`, `supabase` from `../lib/supabaseClient`, `getUnreadCount` from `../lib/notificationPersistence`
- State: `unreadCount` (number, default 0), `panelOpen` (boolean, default false)
- On mount and when `panelOpen` changes to false: call `getUnreadCount(supabase, profile.id)` and set the count. Guard with `if (!profile?.id) return`
- Poll every 30 seconds via `setInterval` for unread count refresh. Clear interval on unmount.
- Render: a `<button>` with an inline SVG bell icon (24x24, stroke-based, matching the existing icon style in Layout.jsx — stroke="#1F2937", strokeWidth=1.8, strokeLinecap="round", strokeLinejoin="round"). Bell path: `M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9`
- Badge: If `unreadCount > 0`, render a small circle positioned absolute `top-0 right-0` with the count. Style: `background: '#EF4444'` (red), `color: '#FFFFFF'`, `fontSize: '10px'`, `fontWeight: 700`, `minWidth: '16px'`, `height: '16px'`, `borderRadius: '9999px'`, `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'center'`, `padding: '0 4px'`. If count > 99, show "99+".
- On button click: toggle `panelOpen`
- When `panelOpen` is true: render `<NotificationPanel onClose={() => setPanelOpen(false)} onCountChange={setUnreadCount} />`

**NotificationPanel.jsx** — Slide-out drawer from the right (portal to document.body):
- Import `createPortal` from `react-dom`, `useAuth`, `supabase`, `getUserNotifications`, `markNotificationRead`, `markAllNotificationsRead` from respective modules
- Props: `onClose` (function), `onCountChange` (function to update bell badge)
- State: `notifications` (array, default []), `loading` (boolean, default true)
- On mount: call `getUserNotifications(supabase, profile.id)` and set notifications. Set loading false.
- Body scroll lock: on mount set `document.body.style.overflow = 'hidden'`, on unmount restore to `''`
- Render via createPortal to document.body:
  - Backdrop: `fixed inset-0`, `background: 'rgba(0,0,0,0.4)'`, `zIndex: 9998`, onClick calls onClose
  - Drawer: `fixed top-0 right-0 h-full`, `width: min(360px, 85vw)`, `background: '#FFFFFF'`, `zIndex: 9999`, `boxShadow: '-4px 0 24px rgba(0,0,0,0.15)'`, transition with CSS `transform` (slide in from right). Use inline style `transform: 'translateX(0)'` with CSS transition `transition: 'transform 0.3s ease-out'`.
  - Header inside drawer: height 56px, flex between, `paddingX 16px`, border-bottom `1px solid #E0E2E6`:
    - Left: `<span>` "Notificaciones" (fontSize 16px, fontWeight 600, color '#1F2937')
    - Right: close X button (inline SVG, `M6 18L18 6M6 6l12 12`, stroke="#6B7280", strokeWidth=2, w-5 h-5)
  - Below header, if any unread notifications exist: a "Marcar todo como leido" button (text button, color '#6BB3D9', fontSize 13px, fontWeight 500, padding 8px 16px, text-right alignment, full width). On click: call `markAllNotificationsRead(supabase, profile.id)`, then update local state: set all notifications `.read = true`, call `onCountChange(0)`.
  - Scrollable list: `overflow-y: auto`, `flex: 1`, padding 8px. Map over `notifications`:
    - Each notification item: padding 12px 16px, borderBottom `1px solid #F3F4F6`, cursor pointer, `background: notification.read ? '#FFFFFF' : '#F0F7FF'` (light blue tint for unread)
    - Left side: small dot indicator for unread — a 8px circle, `background: '#3B82F6'`, borderRadius 9999, marginRight 10px, `visibility: notification.read ? 'hidden' : 'visible'`
    - Layout: flex row, dot on left, content on right (flex-1)
    - Content: message text (fontSize 13px, color '#374151', lineHeight 1.4), below it a timestamp line (fontSize 11px, color '#9CA3AF', marginTop 4px) showing relative time in Spanish. Format: "Hace X min" / "Hace X h" / "Hace X d" / "DD/MM/YYYY". Use a helper function `formatRelativeTime(createdAt)`:
      - `< 60 min` -> "Hace {n} min"
      - `< 24 h` -> "Hace {n} h"  
      - `< 7 d` -> "Hace {n} d"
      - else -> `new Date(createdAt).toLocaleDateString('es-ES')` (DD/MM/YYYY)
    - Type icon: before the message text, render a small colored indicator based on `notification.type`:
      - `'setback'`: orange dot (background '#F59E0B', 6px circle) + text "Contratiempo" in orange (fontSize 10px, fontWeight 600, color '#F59E0B', marginBottom 2px)
      - `'schedule_change'`: green dot (background '#10B981', 6px circle) + text "Cambio de horario" in green (fontSize 10px, fontWeight 600, color '#10B981', marginBottom 2px)
      - `'general'`: blue dot (background '#3B82F6') + text "General" (fontSize 10px, fontWeight 600, color '#3B82F6', marginBottom 2px)
    - On click of a notification item: if `!notification.read`, call `markNotificationRead(supabase, notification.id)`, update local state to set that notification's `.read = true`, call `onCountChange(prev => Math.max(0, prev - 1))`
  - Empty state: if `notifications.length === 0` and `!loading`, render centered div with text "No tienes notificaciones" (fontSize 14px, color '#9CA3AF', textAlign center, paddingTop 48px)
  - Loading state: if `loading`, render centered `<BrandLoader size={32} />` (import from `./BrandLoader`)
  </action>
  <verify>
    <automated>cd "C:/Users/chech/Desktop/Aplicaciones/Torneos Hermandad Gallega/racket-tourneys" && npx eslint src/components/NotificationBell.jsx src/components/NotificationPanel.jsx --no-error-on-unmatched-pattern 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NotificationBell.jsx exists and exports a default function component
    - src/components/NotificationBell.jsx contains `import { getUnreadCount }` from notificationPersistence
    - src/components/NotificationBell.jsx contains `setInterval` for 30-second polling
    - src/components/NotificationBell.jsx contains `unreadCount > 0` for conditional badge rendering
    - src/components/NotificationBell.jsx contains `NotificationPanel` import and conditional render
    - src/components/NotificationPanel.jsx exists and exports a default function component
    - src/components/NotificationPanel.jsx contains `createPortal` from react-dom
    - src/components/NotificationPanel.jsx contains `getUserNotifications` import
    - src/components/NotificationPanel.jsx contains `markNotificationRead` import
    - src/components/NotificationPanel.jsx contains `markAllNotificationsRead` import
    - src/components/NotificationPanel.jsx contains `document.body.style.overflow` for scroll lock
    - src/components/NotificationPanel.jsx contains `Marcar todo como leido` string
    - src/components/NotificationPanel.jsx contains `No tienes notificaciones` string
    - src/components/NotificationPanel.jsx contains `formatRelativeTime` helper function
    - src/components/NotificationPanel.jsx contains `Contratiempo` and `Cambio de horario` type labels
  </acceptance_criteria>
  <done>NotificationBell renders bell SVG with unread count badge (red circle, "99+" cap), polls every 30s, toggles NotificationPanel. NotificationPanel is a portal-based slide-out drawer listing notifications with type labels, relative timestamps in Spanish, read/unread visual distinction (blue tint + dot), tap-to-mark-read, bulk mark-all-read button, and empty state message.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate NotificationBell into Layout.jsx header</name>
  <files>src/components/Layout.jsx</files>
  <read_first>
    - src/components/Layout.jsx
    - src/components/NotificationBell.jsx
  </read_first>
  <action>
Modify Layout.jsx to add the NotificationBell to the header bar, positioned to the LEFT of the "Comision de Fronton" text per D-01.

1. Add import at the top: `import NotificationBell from './NotificationBell'`

2. In the header's inner div (line 140, the `flex items-center justify-between h-14` div), between the page title span and the identity div, add the NotificationBell. Restructure the right side to include bell + identity:

Replace the existing right-side div (lines 153-176) with:
```jsx
{/* Right side: bell + identity */}
<div className="flex items-center gap-3">
  <NotificationBell />
  <span
    style={{
      color:    '#6B7280',
      fontSize: '11px',
      fontWeight: 500,
      letterSpacing: '0.01em',
      whiteSpace: 'nowrap',
    }}
  >
    Comision de Fronton
  </span>
  <img
    src={lobo}
    alt="HGV"
    style={{
      width:     'auto',
      height:    '32px',
      objectFit: 'contain',
      display:   'block',
    }}
  />
</div>
```

This places the bell icon to the left of the "Comision de Fronton" text, naturally in the header's right cluster. Both player and organizer roles see it (per D-06). The existing header structure is preserved — only the right-side div gains the NotificationBell component.
  </action>
  <verify>
    <automated>cd "C:/Users/chech/Desktop/Aplicaciones/Torneos Hermandad Gallega/racket-tourneys" && npx eslint src/components/Layout.jsx --no-error-on-unmatched-pattern 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Layout.jsx contains `import NotificationBell from './NotificationBell'`
    - src/components/Layout.jsx contains `<NotificationBell` rendered inside the header
    - src/components/Layout.jsx contains `gap-3` in the right-side flex container
    - src/components/Layout.jsx still contains `Comision de Fronton` text
    - src/components/Layout.jsx still contains the lobo img element
  </acceptance_criteria>
  <done>NotificationBell appears in the header bar to the left of "Comision de Fronton" text. All roles see the bell. Layout renders correctly with the new component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify notification bell and panel in browser</name>
  <what-built>Bell icon with unread badge in header, slide-out notification panel with read/unread distinction, mark-as-read functionality, empty state, and type-based labels (Contratiempo/Cambio de horario)</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. Log in as any user (player or organizer)
    3. Verify the bell icon appears in the header bar, to the left of "Comision de Fronton"
    4. If there are no notifications, the bell should have NO red badge
    5. Click the bell — a drawer should slide in from the right
    6. If no notifications exist, it should show "No tienes notificaciones"
    7. Close the drawer by clicking the X or the backdrop
    8. To test with notifications: log in as the organizer (6b313573-06cb-4a55-9e76-ce9ca1d68ffd), go to an active tournament, declare a setback on a court, then log in as a player with matches on that court
    9. The player should see a red badge on the bell with the unread count
    10. Open the panel — notifications should show with blue tint (unread), type label (e.g., "Contratiempo" in orange), message text, and relative timestamp
    11. Tap a notification — it should mark as read (blue tint disappears, dot hides, badge count decreases)
    12. Tap "Marcar todo como leido" — all notifications should become read, badge should disappear
  </how-to-verify>
  <files>n/a</files>
  <action>Human verifies notification bell and panel in browser following the steps in how-to-verify above.</action>
  <verify>Visual inspection in browser</verify>
  <done>User confirms bell icon, unread badge, drawer, mark-as-read, and empty state all work correctly</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx eslint src/components/Layout.jsx src/components/NotificationBell.jsx src/components/NotificationPanel.jsx` passes
- Bell icon visible in header for all authenticated roles
- Panel opens/closes with slide animation
- Unread badge shows correct count from database
- Mark-as-read updates both local UI and database
</verification>

<success_criteria>
- Bell icon with red unread count badge renders in Layout header for player, organizer, and admin roles
- Clicking bell opens slide-out drawer with chronological notification list
- Notifications display type label, message, relative timestamp, and read/unread visual distinction
- Individual tap marks notification read; bulk "Marcar todo como leido" marks all read
- Empty state shows "No tienes notificaciones"
- Badge count refreshes on panel close and every 30 seconds
- No regressions in existing header layout or bottom navigation
</success_criteria>

<output>
After completion, create `.planning/phases/08-notificaciones/08-01-SUMMARY.md`
</output>
