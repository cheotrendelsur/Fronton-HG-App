---
phase: 01-design-tokens-config-base
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - vite.config.js
autonomous: true
requirements:
  - TOKEN-05
  - TOKEN-06

must_haves:
  truths:
    - "index.html shows 'Fronton HGV' as the page title"
    - "The browser theme-color bar uses #F2F3F5 (light pearl)"
    - "PWA manifest declares name 'Fronton HGV' and short_name 'Fronton'"
    - "PWA manifest theme_color is #F2F3F5"
  artifacts:
    - path: "index.html"
      provides: "Updated HTML metadata for Fronton HGV brand"
      contains: "Fronton HGV"
    - path: "vite.config.js"
      provides: "Updated PWA manifest metadata"
      contains: "Fronton HGV"
  key_links:
    - from: "vite.config.js"
      to: "index.html"
      via: "PWA manifest generation — theme_color must match in both files"
      pattern: "theme_color.*F2F3F5"
---

<objective>
Update PWA metadata and HTML branding from "RacketTourneys" to "Fronton HGV" with the new pearl theme-color.

Purpose: Aligns the app identity in browser tabs, PWA install prompts, and mobile status bars with the Fronton HGV brand. The theme-color change from dark (#0D1017) to pearl (#F2F3F5) matches the new light interior zone that users see most of the time.

Output: Updated `index.html` and `vite.config.js` with correct brand name and theme-color.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-tokens-config-base/01-CONTEXT.md
@index.html
@vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.html metadata for Fronton HGV</name>
  <files>index.html</files>
  <action>
Make these exact changes to `index.html` (per TOKEN-05):

1. Change `<title>RacketTourneys</title>` to `<title>Frontón HGV</title>`
2. Change `<meta name="description" content="Gestión de torneos de deportes de raqueta" />` to `<meta name="description" content="Gestión de torneos de frontón — Hermandad Gallega de Venezuela" />`
3. Change `<meta name="theme-color" content="#0D1017" />` to `<meta name="theme-color" content="#F2F3F5" />`
4. Change `<meta name="apple-mobile-web-app-title" content="Tourneys" />` to `<meta name="apple-mobile-web-app-title" content="Frontón" />`

**What NOT to change:** All link tags (favicon, icons, manifest, fonts), viewport meta, charset, apple-mobile-web-app-capable, status-bar-style, body content, script tag. Keep the exact same HTML structure and formatting.
  </action>
  <verify>
    <automated>cd "C:\Users\chech\Desktop\Aplicaciones\Torneos Hermandad Gallega\racket-tourneys" && grep "Frontón HGV" index.html && grep 'content="#F2F3F5"' index.html && grep 'content="Frontón"' index.html</automated>
  </verify>
  <done>index.html title is "Fronton HGV", theme-color is #F2F3F5, apple-mobile-web-app-title is "Fronton", description references fronton.</done>
</task>

<task type="auto">
  <name>Task 2: Update vite.config.js PWA manifest metadata</name>
  <files>vite.config.js</files>
  <action>
Make these exact changes to the `manifest` object in `vite.config.js` (per TOKEN-06):

1. Change `name: 'RacketTourneys'` to `name: 'Frontón HGV'`
2. Change `short_name: 'Tourneys'` to `short_name: 'Frontón'`
3. Change `description: 'Gestión de torneos de deportes de raqueta'` to `description: 'Gestión de torneos de frontón — Hermandad Gallega de Venezuela'`
4. Change `theme_color: '#0D1017'` to `theme_color: '#F2F3F5'`
5. Change `background_color: '#0D1017'` to `background_color: '#1E2024'` (keep dark for first-screen since auth is the dark zone users see on cold launch)

**What NOT to change:** The icon entries (those will be updated in Phase 7 when new icon assets are ready), workbox config, registerType, includeAssets, display, orientation, scope, start_url. Keep all other config identical.

Note: The `purpose: 'any maskable'` combined strings on icon entries are a known issue (Lighthouse audit failure) but are deferred to Phase 7 when icons are replaced. Do NOT change icon entries in this phase.
  </action>
  <verify>
    <automated>cd "C:\Users\chech\Desktop\Aplicaciones\Torneos Hermandad Gallega\racket-tourneys" && grep "Frontón HGV" vite.config.js && grep "Frontón'" vite.config.js && grep "#F2F3F5" vite.config.js && grep "#1E2024" vite.config.js</automated>
  </verify>
  <done>vite.config.js manifest has name "Fronton HGV", short_name "Fronton", theme_color "#F2F3F5", background_color "#1E2024". Description updated to reference fronton.</done>
</task>

</tasks>

<verification>
Verify both files have synchronized theme-color and brand name:

```bash
cd "C:\Users\chech\Desktop\Aplicaciones\Torneos Hermandad Gallega\racket-tourneys"
grep "F2F3F5" index.html && grep "F2F3F5" vite.config.js && echo "theme-color synced"
grep "Frontón" index.html && grep "Frontón" vite.config.js && echo "brand name synced"
```

Run build to confirm no syntax errors introduced:
```bash
npm run build
```
</verification>

<success_criteria>
1. `index.html` title is "Fronton HGV"
2. `index.html` theme-color meta is #F2F3F5
3. `index.html` apple-mobile-web-app-title is "Fronton"
4. `vite.config.js` manifest name is "Fronton HGV"
5. `vite.config.js` manifest short_name is "Fronton"
6. `vite.config.js` manifest theme_color is #F2F3F5
7. `vite.config.js` manifest background_color is #1E2024
8. `npm run build` completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-tokens-config-base/01-02-SUMMARY.md`
</output>
